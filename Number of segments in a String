// NUmber of segments of a string
int main()
{
	int i,j=1,k;
	char a[1000];
	gets(a);
	k=strlen(a);
	int c=0;
	for(i=0;a[i]!='\0';i++)
	{
		if(a[i]==32)
		{
			j++;
		}
	}
	for(i=0;i<k;i++)
	{
		if(a[i]=='\0')
		{
			c++;
		}
	}
	if(c==k)
	printf("0");
	else
	printf("%d",j);
}
